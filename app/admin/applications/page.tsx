import { requireAdmin } from "@/lib/auth";

type Application = { id: string; course_title: string; committed_price: string; application_data: { first_name?: string; last_name?: string; email?: string; phone?: string }; medical_document_path?: string | null; status: string; created_at: string };
export default async function ApplicationsPage() { const { supabase } = await requireAdmin(); const { data } = await supabase.from("applications").select("*").order("created_at", { ascending: false }); const applications = (data ?? []) as Application[]; return <div><h1 className="text-4xl font-bold">Jelentkezések</h1><p className="mt-2 text-slate-600">Az itt megjelenő adatok személyes adatokat tartalmaznak. Csak jogosult munkatársak számára hozzáférhetők.</p><div className="mt-8 grid gap-4">{applications.length === 0 ? <p className="rounded-2xl bg-white p-6 shadow">Még nincs beküldött jelentkezés.</p> : applications.map((application) => <article key={application.id} className="rounded-2xl bg-white p-6 shadow"><div className="flex flex-wrap items-start justify-between gap-4"><div><h2 className="text-xl font-bold">{application.application_data.last_name} {application.application_data.first_name}</h2><p className="mt-1 text-sm text-slate-600">{application.course_title} · {application.committed_price}</p><p className="mt-3 text-sm">{application.application_data.email} · {application.application_data.phone}</p>{application.medical_document_path && <a href={`/admin/applications/${application.id}/medical-document`} target="_blank" rel="noreferrer" className="mt-4 inline-flex rounded-lg bg-slate-900 px-3 py-2 text-sm font-bold text-white transition hover:bg-amber-500 hover:text-black">Orvosi alkalmassági megnyitása</a>}</div><time className="text-sm text-slate-500">{new Intl.DateTimeFormat("hu-HU", { dateStyle: "medium", timeStyle: "short" }).format(new Date(application.created_at))}</time></div></article>)}</div></div>; }
